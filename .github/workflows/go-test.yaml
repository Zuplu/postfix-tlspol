# Made with ❤️ by Zuplu.
# All rights reserved.

name: 🧪 Run unit tests

on:
  push:
    paths: "**/*.go"
  pull_request:
  workflow_call:

permissions:
  contents: read

jobs:
  test:
    name: 🕵🏻‍♂️ Analyze Compiling, DANE and MTA-STS Lookup
    runs-on: ubuntu-latest
    steps:
      - name: 📦 Checkout repository
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # pin@v4
      - name: ⚙️ Set up Go
        uses: actions/setup-go@7a3fe6cf4cb3a834922a1244abfce67bcef6a0c5 # pin@v5
        with:
          go-version-file: ./go.mod
      - name: 🧐 Test basic functionality
        run: go test -v -count=1 -failfast -tags netgo ./...
